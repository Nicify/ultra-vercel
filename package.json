{
  "scripts": {
    "build": "mkdir -p .vercel_build_output/static && cp -a src/. .vercel_build_output/static && cp -a .ultra/. .vercel_build_output/static",
    "ultra": "deno run --allow-all --unstable build.js"
  }
}
